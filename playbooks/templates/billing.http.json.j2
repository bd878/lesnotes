{
  "node_name": "billing.http",
  "billing_service_addr": "{{ addr }}:{{ billing_port }}",
  "http_addr": "{{ addr }}:{{ billing_http_port }}",
  "nats_addr": "{{ nats }}",
  "queue_group": "billing",
  "users_service_addr": "{{ addr }}:{{ users_port }}",
  "sessions_service_addr": "{{ addr }}:{{ sessions_port }}",
  "log_level": "{{ log_level }}"
}
