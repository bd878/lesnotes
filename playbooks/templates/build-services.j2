#!/usr/bin/bash
  
oval=${1?"Usage: $0 oval nval"}
nval=${2?"Usage: $0 oval nval"}

# env BUILD_DIR, REPO, LOG, ENV

exec 7<&1
exec &> $LOG

cd $REPO
printf "*** PWD = %s\n" $PWD
printf "*** ENV = %s\n" $ENV
printf "*** BUILD_DIR = %s\n" $BUILD_DIR
echo

export GIT_DIR="$(pwd)/.git"

git diff --quiet "$oval" "$nval" -- "front"
if (( $? > 0 ))
then
    echo "*** building front"
    echo
    setsid -f bash --login -c "env BUILD_DIR=$BUILD_DIR ENV=$ENV /srv/misc/scripts/build-front"
else
    echo "*** no changes in front"
    echo
fi

git diff --quiet "$oval" "$nval" -- "server/messages"
if (( $? > 0 ))
then
    echo "*** building messages"
    echo
    setsid -f bash --login -c "env BUILD_DIR=$BUILD_DIR ENV=$ENV /srv/misc/scripts/build-messages"
else
    echo "*** no changes in messages"
    echo
fi

git diff --quiet "$oval" "$nval" -- "server/db/messages"
if (( $? > 0 ))
then
    echo "*** building messages db"
    echo
    setsid -f bash --login -c "env BUILD_DIR=$BUILD_DIR ENV=$ENV /srv/misc/scripts/build-messages-db"
else
    echo "*** no changes in messages db"
    echo
fi

git diff --quiet "$oval" "$nval" -- "server/users"
if (( $? > 0 ))
then
    echo "*** building users"
    echo
    setsid -f bash --login -c "env BUILD_DIR=$BUILD_DIR ENV=$ENV /srv/misc/scripts/build-users"
else
    echo "*** no changes in users"
    echo
fi

git diff --quiet "$oval" "$nval" -- "server/db/users"
if (( $? > 0 ))
then
    echo "*** building users db"
    echo
    setsid -f bash --login -c "env BUILD_DIR=$BUILD_DIR ENV=$ENV /srv/misc/scripts/build-users-db"
else
    echo "*** no changes in users db"
    echo
fi

git diff --quiet "$oval" "$nval" -- "server/files"
if (( $? > 0 ))
then
    echo "*** building files"
    echo
    setsid -f bash --login -c "env BUILD_DIR=$BUILD_DIR ENV=$ENV /srv/misc/scripts/build-files"
else
    echo "*** no changes in files"
    echo
fi

git diff --quiet "$oval" "$nval" -- "server/db/sessions"
if (( $? > 0 ))
then
    echo "*** building sessions db"
    echo
    setsid -f bash --login -c "env BUILD_DIR=$BUILD_DIR ENV=$ENV /srv/misc/scripts/build-sessions-db"
else
    echo "*** no changes in sessions db"
    echo
fi

git diff --quiet "$oval" "$nval" -- "server/search"
if (( $? > 0 ))
then
    echo "*** building search"
    echo
    setsid -f bash --login -c "env BUILD_DIR=$BUILD_DIR ENV=$ENV /srv/misc/scripts/build-search"
else
    echo "*** no changes in search"
    echo
fi

git diff --quiet "$oval" "$nval" -- "server/db/search"
if (( $? > 0 ))
then
    echo "*** building search db"
    echo
    setsid -f bash --login -c "env BUILD_DIR=$BUILD_DIR ENV=$ENV /srv/misc/scripts/build-search-db"
else
    echo "*** no changes in search db"
    echo
fi

git diff --quiet "$oval" "$nval" -- "server/threads"
if (( $? > 0 ))
then
    echo "*** building threads"
    echo
    setsid -f bash --login -c "env BUILD_DIR=$BUILD_DIR ENV=$ENV /srv/misc/scripts/build-threads"
else
    echo "*** no changes in threads"
    echo
fi

git diff --quiet "$oval" "$nval" -- "server/db/threads"
if (( $? > 0 ))
then
    echo "*** building threads db"
    echo
    setsid -f bash --login -c "env BUILD_DIR=$BUILD_DIR ENV=$ENV /srv/misc/scripts/build-threads-db"
else
    echo "*** no changes in threads db"
    echo
fi

git diff --quiet "$oval" "$nval" -- "server/billing"
if (( $? > 0 ))
then
    echo "*** building billing"
    echo
    setsid -f bash --login -c "env BUILD_DIR=$BUILD_DIR ENV=$ENV /srv/misc/scripts/build-billing"
else
    echo "*** no changes in billing"
    echo
fi

git diff --quiet "$oval" "$nval" -- "server/db/billing"
if (( $? > 0 ))
then
    echo "*** building billing db"
    echo
    setsid -f bash --login -c "env BUILD_DIR=$BUILD_DIR ENV=$ENV /srv/misc/scripts/build-billing-db"
else
    echo "*** no changes in billing db"
    echo
fi

exec 1<&7 7<&-

exit 0;

