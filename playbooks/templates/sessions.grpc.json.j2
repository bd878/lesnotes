{ 
  "node_name": "sessions.{{ sessions_port }}",
  "rpc_addr": "{{ addr }}:{{ sessions_port }}",
  "serf_addr": "{{ addr }}:{{ sessions_serf_port }}",
  "serf_join_addrs": ["{{ join_addr }}:{{ sessions_join_port }}"],
  "goose_table_name": "sessions_goose_db_version",
  "raft_bootstrap": {{ raft_bootstrap | string | lower }},
  "raft_log_level": "{{ log_level }}",
  "log_level": "{{ log_level }}",
  "data_path": "../data.{{ sessions_port }}",
  "nats_addr": "{{ nats }}",
  "pg_conn": "{{ pg_conn }}"
}
