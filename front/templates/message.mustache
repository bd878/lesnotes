<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<link rel="icon" href="/public/favicon.ico">
		<meta name="viewport" content="width=device-width,initial-scale=1" />
		{{#styles}}
			<link rel="stylesheet" href="{{.}}">
		{{/styles}}
		<title>Les notes</title>
	</head>
	<body data-id="{{id}}" data-user-id="{{user}}">
		<div class="whitespace-pre">{{message.text}}</div>

		{{#files}}
			<a css="underline text-blue-600 visited:text-purple-600" href="/files/v2/{{name}}" target="_blank" download="{{name}}">{{name}}</a>
		{{/files}}
	</body>
</html>
