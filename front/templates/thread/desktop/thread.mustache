<div class="h-full max-w-full flex flex-row">
	<div class="flex flex-col overflow-x-hidden mr-8 w-full min-w-sm max-w-lg">
		{{> header}}
		<div class="grow h-0 overflow-y-hidden">
			{{> messagesTree}}
		</div>
		{{> controlPanel}}
	</div>

	{{#hasContent}}
		<div class="w-full min-w-md max-w-2xl flex flex-row mr-8">
			<div class="flex flex-col grow-2 w-full">
				<div class="flex flex-col grow p-2 border border-1 border-solid rounded-md h-full">
					{{> messageView}}
					{{> threadView}}
				</div>
			</div>
		</div>
	{{/hasContent}}

	<div class="h-full flex flex-col max-w-2xl min-w-md w-full overflow-x-hidden">
		{{> threadFeatures}}
		{{> messageFeatures}}
	</div>
</div>