<div class="h-full max-w-full flex flex-row">
	<div class="flex flex-col overflow-x-hidden mr-8 min-w-md max-w-md">
		{{> navigation}}
		<div class="h-full overflow-y-hidden">
			{{> messagesStack}}
		</div>
		{{> controlPanel}}
	</div>

	<div class="min-w-xl max-w-xl flex flex-row mr-8">
		<div class="flex flex-col grow-2 w-full">
			{{> translations}}
			<div class="flex flex-col grow p-2 border border-1 border-solid rounded-md h-full">
				{{> messageEditForm}}
				{{> messageView}}
				{{> newMessageForm}}
				{{> translationEditForm}}
				{{> translationView}}
				{{> newTranslationForm}}
				{{> threadEditForm}}
				{{> threadView}}
				{{> filesList}}
				{{> pagination}}
				{{> filesForm}}
			</div>
		</div>
	</div>

	<div class="h-full flex flex-col max-w-md min-w-md overflow-x-hidden">
		{{> messageNavigation}}
		{{> filesView}}
		{{> comments}}
	</div>
</div>