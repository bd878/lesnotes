<!DOCTYPE html>
{{#html}}
	<head>
		<meta charset="UTF-8" />
		<link rel="icon" href="/public/favicon.ico">
		<meta name="viewport" content="width=device-width,initial-scale=1" />

		{{#styles}}
			<style type="text/css">{{{styles}}}</style>
		{{/styles}}

		{{#manifest}}
			<link rel="manifest" href={{{.}}} />
		{{/manifest}}

		<title>Les notes</title>
	</head>
	<body class="large:text-xl small:text-sm dark:bg-gray-900 dark:text-gray-400 text-gray-950">
		<div data-is-mobile="true" class="py-5 h-screen flex flex-col items-start relative">
			<div class="w-full grow">
				{{> content}}
			</div>

			<div class="pt-5 pb-5 w-full">
				{{> footer}}
			</div>
		</div>

		{{#scripts}}
			<script type="module" src="{{{.}}}"></script>
		{{/scripts}}
	</body>
{{/html}}